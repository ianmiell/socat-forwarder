apiVersion: v1
kind: BuildConfig
metadata:
  labels:
    app: socat-forwarder
  name: socat-forwarder
spec:
  resources: {}
  source:
    git:
      ref: master
      uri: https://github.com/ianmiell/socat-forwarder
    type: Git
  strategy:
    type: Docker
